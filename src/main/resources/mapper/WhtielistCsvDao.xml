<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab8.galaxy.dao.WhtielistCsvDao">

    <resultMap type="com.lab8.galaxy.entity.WhtielistCsv" id="WhtielistCsvMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="addr" column="addr" jdbcType="VARCHAR"/>
        <result property="share" column="share" jdbcType="INTEGER"/>
        <result property="pid" column="pid" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>

    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="WhtielistCsvMap">
        select
          id, addr, pid, status,share
        from whitelist_csv
        where id = #{id}
    </select>
    <!--查询单个-->
    <select id="queryByPid" resultType="java.util.Date">
        select
            created_at
        from whitelist_csv
        where pid = #{pid} and status = 1
        order by created_at DESC
            limit 1
    </select>


    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="WhtielistCsvMap">
        select
          id, addr, pid, status,share
        from whitelist_csv
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="addr != null and addr != ''">
                and addr = #{addr}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <select id="queryByWhitelist" resultMap="WhtielistCsvMap">
        select
        id, addr, pid, status,share
        from whitelist_csv
        <where>
            <if test="addr != null and addr != ''">
                and addr = #{addr}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            and status = 1
        </where>
        limit 1
    </select>
    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from whitelist_csv
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="addr != null and addr != ''">
                and addr = #{addr}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <!--统计总行数-->
    <select id="existByStatus" resultType="java.lang.Long">
        select count(1)
        from whitelist_csv
        <where>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into whitelist_csv(addr, pid, status)
        values (#{addr}, #{pid}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into whitelist_csv(addr, pid, status,created_at,share)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.addr}, #{entity.pid}, #{entity.status},NOW(),#{entity.share})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into whitelist_csv(addr, pid, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.addr}, #{entity.pid}, #{entity.status})
        </foreach>
        on duplicate key update
        addr = values(addr),
        pid = values(pid),
        status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update whitelist_csv
        <set>
            <if test="addr != null and addr != ''">
                addr = #{addr},
            </if>
            <if test="pid != null">
                pid = #{pid},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>
    <!--通过主键修改数据-->
    <update id="updateByStatus">
        update whitelist_csv
        <set>
                status = #{updateStatus},
        </set>
        where pid = #{pid} and status = #{status}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from whitelist_csv where id = #{id}
    </delete>
    <!--通过主键删除-->
    <delete id="deleteDataWithStatus3">
        delete from whitelist_csv where pid = #{pid} and status =#{status}
    </delete>

</mapper>

